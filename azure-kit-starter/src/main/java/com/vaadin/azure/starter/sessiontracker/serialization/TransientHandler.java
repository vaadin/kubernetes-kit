package com.vaadin.azure.starter.sessiontracker.serialization;

/**
 * A combination of inspector and injector.
 * 
 * TODO: maybe just drop extended interfaces and merge them into this one
 */
public interface TransientHandler
        extends TransientInspector, TransientInjector {
}

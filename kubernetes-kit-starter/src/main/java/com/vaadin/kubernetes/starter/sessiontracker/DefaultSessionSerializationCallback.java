package com.vaadin.kubernetes.starter.sessiontracker;

/**
 * Default error callbacks that are called after a serialization or
 * deserialization error happens.
 */
public class DefaultSessionSerializationCallback
        implements SessionSerializationCallback {
}

apiVersion: v1
kind: Service
metadata:
  name: kubernetes-kit-hazelcast-service
  namespace: canary
spec:
  selector:
    app: kubernetes-kit-demo
  ports:
    - name: hazelcast
      port: 5701
  type: LoadBalancer

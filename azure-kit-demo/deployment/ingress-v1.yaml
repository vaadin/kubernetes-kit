apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: azure-kit-demo
  namespace: canary
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/affinity: "cookie"
    nginx.ingress.kubernetes.io/affinity-mode: "persistent"
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: azure-kit-demo-v1
                port:
                  number: 80
